# 2026-04-04

## Analyse Spike vs SimpleActivity (exports Temporal)

- **Contexte** : comparaison des JSON d'historique `e5925b43…` (NativeExecutionSpike) et `4455cf9c…` (Samples SimpleActivity via DurableBundle).
- **Constat** : le spike produit la chaîne Temporal attendue (activité + complétion) ; SimpleActivity n'a qu'une première workflow task et un input `ExecutionStarted` sans bootstrap `temporalNativeSchedule` dans l'extrait — aligné avec interpréteur sans commandes et risque d'alias DI `NullExecutionStartedPayloadExtension` vs `SamplesTemporalNativeExecutionStartedExtension`.
- **Conclusion** : écart structurel entre journal domaine PHP et orchestration visible Temporal (DUR019/DUR026) ; les samples Symfony compensent par extension de payload, pas par généralisation du moteur seul.

## Nettoyage final et tests WorkflowTaskRunner

Suite des tâches optionnelles proposées :

1. **Suppression WorkflowRunMessage / WorkflowRunHandler** — classes de transition supprimées. Le profiler (`WorkflowRunDispatchProfilerMiddleware`, `DurableProfilerEventPresentation`) mis à jour pour ne référencer que `ResumeWorkflowMessage`. Routing Messenger nettoyé. Tests Symfony repassent (37/37, 9 skips).

2. **Tests unitaires WorkflowTaskRunner** — 2 nouveaux fichiers dans `tests/unit/Bridge/Temporal/Worker/` :
   - `TemporalExecutionHistoryTest` (11 tests) : vérifie le parsing des événements Temporal Protobuf et les lookups de slots (activités, timers, signaux).
   - `WorkflowTaskRunnerTest` (10 tests) : vérifie le replay Fiber complet — workflow qui complète immédiatement, replay d'activité, nouvelle activité (commande ScheduleActivity), activité échouée (commande FailWorkflow), activités parallèles, timers rejoués, timers nouveaux, exception dans le handler, signal rejoué, signal en attente.
   - `WorkflowRegistry.registerFactory()` ajouté pour permettre l'enregistrement d'inline handlers dans les tests.

3. **README Symfony** mis à jour : documentation de `WorkflowClient`, `ResumeWorkflowMessage`, `WorkflowTaskRunner`, `TemporalHistoryCursor` et configuration bundle sans `interpreter_mirror_activities`.
