# Journal — 2026-04-02

## Création des ADR Durable (DUR000–DUR006)

- **Demande** : définir un jeu d’ADR pour le composant Durable, en s’inspirant des principes connus (gestion ADR, CQRS, curseurs, workflows/activités Temporal) sans référencer les fichiers d’origine dans les textes publiés ; ne pas démarrer l’implémentation.
- **Décision** : préfixe `DUR`, fichiers sous `documentation/adr/` ; index mis à jour ; `documentation/archive/` ajouté au `.gitignore` pour que l’archive locale ne soit pas versionnée.
- **Contenu** : EventStore + curseur ; repos Command/Query ; StateMachine + replay + fibers + contexte awaitables + idempotence ; ActivityStub + `#[ActivityMethod]` ; backends Temporal + In-Memory ; interdiction SDK officiel / RoadRunner (alignée règle Cursor existante).

## ADR sérialisation, PER, tests (DUR007–DUR010)

- **Demande** : sérialisation via le Serializer Symfony ; nommage selon le PER PHP-FIG (dernière version) ; ADR sur les règles de tests et la pyramide des tests.
- **Décision** : DUR007 (Symfony Serializer) ; DUR008 (PER, lien officiel php-fig.org) ; DUR009 (PHPUnit, déterminisme, doubles, In-Memory, lien DUR010) ; DUR010 (pyramide : unitaire / intégration In-Memory / E2E Temporal minoritaire). DUR004 mis à jour pour pointer vers DUR007.

## ADR erreurs, couches, workflows, intégrations, tests, in-memory, observabilité (DUR011–DUR017)

- **Demande** : ajouter des ADR pour la gestion des erreurs et retries ; client API vs adaptateur repository ; modélisation des workflows et Query/Signal/Update ; cas limites temporels et intégrations ; tests repositories/données ; exceptions In-Memory ; observabilité et opérations ; en réutilisant les principes issus de l’analyse des anciennes ADR encore présentes localement (hors dépôt).
- **Décision** : sept ADR créés ; **DUR002** complété (renvois DUR011, DUR012, DUR015). **DUR013** : constructeur avec contexte runtime Durable (explicitement distinct d’un modèle SDK imposant un constructeur vide).
