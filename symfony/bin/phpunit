#!/usr/bin/env php
<?php

$phpunit = dirname(__DIR__).'/vendor/phpunit/phpunit/phpunit';
if (!is_file($phpunit)) {
    fwrite(STDERR, "PHPUnit not found. Run: cd symfony && composer install\n");
    exit(1);
}
require $phpunit;
