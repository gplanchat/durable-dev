# refactor/fold-temporal-http

- **Chantier** : #428 — fold `durable-bridge-temporal-http` into `durable-bridge-temporal` (`Gplanchat\Bridge\Temporal\Http`), fallback on `ext-curl`.
- **Entrées** : `src/Bridge/TemporalHttp/`, `src/Bridge/Temporal/`, root and bridge `composer.json`, `composer.lock`, `phpstan.neon`, `psalm.xml`, tests, configuration docs.
- **État** : en relecture — PR #429
