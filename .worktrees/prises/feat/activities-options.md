# feat/activities-options

- **Chantier** : #462 — `#[Activities]` configures the injected stub's `ActivityOptions` (timeouts, retries, queue…), validated at `load()`.
- **Entrées** : `src/Durable/Attribute/Activities.php`, `src/Durable/Workflow/WorkflowDefinitionLoader.php`, their tests, a Temporal integration test, the workflows page (EN + FR).
- **État** : en relecture — PR #463
