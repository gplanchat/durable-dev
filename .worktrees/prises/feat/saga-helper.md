# feat/saga-helper

- **Chantier** : a `Saga` helper that records compensations and runs them in reverse; used in the Symfony RFC (symfony/symfony#66257) and the docs.
- **Entrées** : `src/Durable/Workflow/Saga.php`, its test, `documentation/user/cancellation/`, `documentation/user/comparison/`, `src/DurableRector/`.
- **État** : en relecture (#440)
