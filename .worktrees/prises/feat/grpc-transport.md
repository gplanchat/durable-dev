# feat/grpc-transport

- **Chantier** : #430 (part 1 of #425) — `GrpcTransport`, a service-agnostic gRPC unary transport; ext-grpc and curl adapters; one `WorkflowServiceClient`.
- **Entrées** : `src/Bridge/Temporal/` (clients, `Grpc/`, `Http/`, factory), their unit tests.
- **État** : en cours
