# feat/guzzle-grpc-transport

- **Chantier** : #432 (part of #425) — `GuzzleGrpcTransport`, gRPC over Guzzle >= 7.14; `transport=guzzle`.
- **Entrées** : `src/Bridge/Temporal/Http/`, `WorkflowServiceClientFactory`, `TemporalConnection`, root and bridge `composer.json`, `composer.lock`, unit and integration tests.
- **État** : en relecture — PR #435
