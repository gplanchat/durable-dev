# feat/run-list-waiting-pickup

- **Chantier** : #447 — the run list tells a run waiting to be picked up from a run waiting on a timer (#263)
- **Entrées** : `src/Durable/Observation/`, `src/Durable/Port/WorkflowRunCatalogInterface.php` and its implementations, `src/DurablePlugin` dashboard, `src/DurableModule` process grid, the Symfony bench dashboard, dashboard docs
- **État** : en cours (conception)
