# fix/psalm-couvre-le-pont-illuminate

- **Chantier** : Psalm ne regarde pas `src/Bridge/Illuminate/` — le pont est le seul de `phpstan.neon` absent de `psalm.xml`
- **Entrées** : `psalm.xml`, `psalm-baseline.xml`
- **État** : en cours
