# docs/hugo-says-what-ci-does

- **Scope**: #369, the part outside prise `feat/mesure-d-audience`: WA005 says the CI replay guard
  exists (D-5); a project `hugo-docs/i18n/fr.yaml` for the six missing theme keys (D-22);
  `HUGO.md` says the `.fr.md` pages render under `/fr/docs/` (D-4, doc half); the French
  maintainer files translated (D-3). The `hugo.toml` items (D-4 comment, D-20 mount,
  `languageCode`) wait on that prise; the prune guard in `docs-ovh.yml` needs a human.
- **Entries**: `documentation/wa/WA005-*.md`, `documentation/HUGO.md`, `hugo-docs/i18n/`,
  `hugo-docs/README.md`, `hugo-docs/assets/{_custom,_variables}.scss`, `hugo-docs/assets/landing.js`,
  `hugo-docs/static/.htaccess`, `hugo-docs/import-design.py`, `hugo-docs/layouts/index{,.fr}.html`
  and `hugo-docs/imported.json` (regenerated by the import only, never by hand).
- **Done when**: `/fr/docs/` pages on a `hugo --minify` build served over HTTP show the six labels
  in French; the CI replay (`--self-test`, both `--force` imports, `diff -u`) is empty;
  `grep -nP '[éèàùç]'` is empty on the translated files.
- **State**: in review — PR #509 (hugo.toml items wait on feat/mesure-d-audience) — bob.
