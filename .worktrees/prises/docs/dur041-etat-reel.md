# docs/dur041-etat-reel

- **Chantier** : DUR041 décrit deux dispositions qui n'existent pas — les stores Temporal
  n'étendent aucune suite de conformité, ni au palier port ni au palier replay. Le même énoncé
  faux est repris dans le docbloc de `EventStoreReplayConformanceTestCase`.
- **Entrées** : `documentation/adr/DUR041-*.md` et le docbloc de
  `src/Durable/Testing/EventStoreReplayConformanceTestCase.php`. Aucun test, aucun comportement.
- **État** : rédaction.
