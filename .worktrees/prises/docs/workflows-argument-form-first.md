# docs/workflows-argument-form-first

- **Scope**: #419, its docs box (closed but not met, per the #306 re-audit): the workflows page shows
  the argument form first, EN and FR. The constructor + contract-interface form moves to where the
  page says when to use it; the rules that named the constructor as the only way say both.
- **Entries**: `documentation/user/workflows/_index.md`, `documentation/user/workflows/_index.fr.md`.
- **Done when**: the first example of both pages uses `#[Activities]` arguments and no constructor;
  no line still states the constructor as the only form; the page checked on a minified build served
  over HTTP.
- **State**: in progress — bob, reviewer vera.
