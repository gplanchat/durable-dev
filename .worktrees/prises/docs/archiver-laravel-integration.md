# docs/archiver-laravel-integration

Worktree jetable. Le chantier `laravel-integration` est à 20/20 depuis la fusion
de la #221, sans prise et sans PR ouverte : il ne lui manquait que son geste de
clôture.

Deux choses, et rien d'autre :

1. `openspec/specs/laravel-host/spec.md` — **une capacité neuve**, le répertoire
   n'existait pas. Le delta du change n'avait pas de `## Purpose`, contrairement
   à celui de `temporal-nexus-support` ; il en fallait un, la section étant ce
   que porte toute spec publiée.
2. `openspec/changes/laravel-integration/` → `archive/2026-08-29-laravel-integration/`.

Aucun code touché.
