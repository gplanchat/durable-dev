# docs/concepts-backends-paquets-quatre

- **Chantier** : reprise de la #266, mergée sur une head périmée pendant une panne GitHub — seuls
  ses deux commits `comparison/` sont dans `main`. Les trois autres reviennent par la
  [#267](https://github.com/gplanchat/durable-dev/pull/267) : quatre backends dans `concepts/` et
  `backends/`, et la NOTE « no Laravel integration package yet » de `packages/`.
- **Entrées** : `documentation/user/concepts/`, `documentation/user/backends/`,
  `documentation/user/packages/` — les deux langues de chacune. **Ne pas y toucher en parallèle**,
  un audit de la doc tourne par ailleurs.
- **État** : en revue, CI verte.
