# docs/sylius-plugin-screenshots

- **Work item**: #252, screenshots of the Sylius dashboard in `src/DurablePlugin/README.md`.
- **Entry points**: `src/DurablePlugin/README.md`, images under `src/DurablePlugin/docs/`.
- **Source**: shot from the `sylius/` bench, DBAL journal, seeded runs.
- **Careful**: jane's route split (#264/#383) changes the same pages; shoot after it if it is close.
- **State**: taken, worktree `.worktrees/anna-sylius-screenshots`.
