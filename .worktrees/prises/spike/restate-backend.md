# spike/restate-backend

- **Scope**: #464 — Restate as a third Durable backend, through its request-response service
  protocol. One throwaway workflow (one activity, one sleep, one signal) against a local
  `restate-server` in Docker, and a findings comment on #464.
- **Entries**: `spike/restate/` only — nothing under `src/`, no `composer.json` change, generated
  protobuf code gitignored and regenerated by the spike script.
- **State**: in review — PR #466, findings on #464.
