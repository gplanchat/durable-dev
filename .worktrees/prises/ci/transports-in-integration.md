# ci/transports-in-integration

- **Chantier** : #451 — CI runs the integration suite over ext-grpc and Guzzle, the JSON gateway over PSR-18, and `ci-ok` needs both jobs.
- **Entrées** : `.github/workflows/ci.yml` (approved by the maintainer).
- **État** : en relecture — PR #452
