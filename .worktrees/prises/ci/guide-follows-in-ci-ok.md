# ci/guide-follows-in-ci-ok

- **Chantier** : `guide-follows` joins `ci-ok`'s needs, so a guide that no longer builds blocks a merge.
- **Entrées** : `.github/workflows/ci.yml` (approved by the maintainer).
- **État** : en cours
