# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # Default configuration for services in *this* file
    _defaults:
        # Automatically injects dependencies in your services
        autowire: true

        # Automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true

        # Allows optimizing the container by removing unused services; this also means
        # fetching services directly from the container via $container->get() won't work
        public: false

    _instanceof:
        Sylius\Bundle\ResourceBundle\Controller\ResourceController:
            autowire: false
        Sylius\Bundle\ResourceBundle\Form\Type\AbstractResourceType:
            autowire: false

    # Makes classes in src/ available to be used as services;
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/Entity/'
            - '../src/Kernel.php'

    # Workflows are declared by a tag and not by an attribute: the tag is what WorkflowPass reads.
    # Without it, an #[AsWorkflow] is a class that nobody registers.
    App\Durable\Workflow\:
        resource: '../src/Durable/Workflow/'
        tags: [durable.workflow]

    # Controllers are imported separately to make sure services can be injected
    # as action arguments even if you don't extend any base controller class
    App\Controller\:
        resource: '../src/Controller'
        tags: ['controller.service_arguments']

# The Nexus tag is placed here, and not by `#[AsNexusServiceHandler]` on the class.
#
# The attribute would hold in **every** environment, and a handler declared on a backend with no
# route is refused at boot — rightly so, since it would never receive anything. But the shop only
# has a cluster in its demonstration profile. The tag therefore says what is true: it serves
# `stock` in `demo`, and nowhere else.
#
# `symfony/` does keep the attribute: its bench has a DSN in dev as in prod.
when@demo:
    services:
        # `autowire` explicitly: redefining a service in a `when@` block **replaces** the
        # definition that came from loading `App\`, and the `_defaults` of the main block do not
        # apply to it. Without this line, the constructor receives nothing.
        App\Durable\Nexus\StockHandler:
            autowire: true
            tags:
                - name: durable.nexus_handler
                  contract: 'Gplanchat\Durable\Demo\Contracts\Stock\StockServed'
