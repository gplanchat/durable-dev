<?xml version="1.0"?>
<psalm
    errorLevel="4"
    findUnusedCode="false"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    errorBaseline="psalm-baseline.xml"
>
    <projectFiles>
        <directory name="src"/>
        <directory name="tests"/>
        <ignoreFiles>
            <directory name="src/TemporalGenerated"/>
            <directory name="vendor"/>
            <directory name="src/Bridge/Temporal"/>
            <!-- Tests du bridge : src ignoré ; les tests appellent des API @internal (codec, resolver). -->
            <directory name="tests/unit/Bridge/Temporal"/>
        </ignoreFiles>
    </projectFiles>
    <issueHandlers>
        <!-- PHP 8.2 : pas d’attribut #[\Override] (PHP 8.3+) ; doubles / classes anonymes dans les tests. -->
        <MissingOverrideAttribute>
            <errorLevel type="suppress">
                <directory name="tests"/>
                <directory name="src"/>
            </errorLevel>
        </MissingOverrideAttribute>
    </issueHandlers>
    <plugins>
        <pluginClass class="Gplanchat\Durable\Psalm\Plugin"/>
    </plugins>
</psalm>
