<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
>
    <testsuites>
        <testsuite name="unit">
            <directory>tests/unit</directory>
            <!-- rector/rector embarque son propre nikic/php-parser, non préfixé. Sous couverture,
                 php-code-coverage charge celui du projet et les deux se déclarent : « Cannot declare
                 class PhpParser\Node\Scalar\Float_ ». D'où une suite à part, écartée de la
                 couverture — src/DurableRector n'est de toute façon pas dans <source>. -->
            <exclude>tests/unit/DurableRector</exclude>
        </testsuite>
        <testsuite name="rector">
            <directory>tests/unit/DurableRector</directory>
        </testsuite>
        <!-- Le paquet d'intégration Laravel, isolé pour que la matrice « Laravel x / PHP y »
             puisse l'exercer seule contre une ligne d'illuminate/* épinglée. Il reste dans la
             suite `unit`, donc la QA et la couverture le voient toujours. -->
        <testsuite name="laravel">
            <directory>tests/unit/DurableLaravel</directory>
        </testsuite>
        <testsuite name="plugin">
            <directory>src/DurablePlugin/tests</directory>
        </testsuite>
        <!-- Contre un vrai serveur Temporal ; voir tests/integration/Temporal/TemporalServerTestCase.php -->
        <testsuite name="integration">
            <directory>tests/integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src/Durable</directory>
            <directory>src/DurableBundle</directory>
            <directory>src/Bridge/Temporal</directory>
            <directory>src/Bridge/Dbal</directory>
            <directory>src/DurablePlugin</directory>
        </include>
        <exclude>
            <directory>src/Bridge/Temporal/Api</directory>
            <directory>src/Bridge/Temporal/Generated</directory>
        </exclude>
    </source>
</phpunit>
